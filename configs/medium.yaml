vocab_size: 32000
d_model: 768
n_heads: 12
n_layers: 12
d_ff: 3072
max_seq_len: 1024
dropout: 0.1
batch_size: 8
max_epochs: 10
learning_rate: 0.0003
warmup_steps: 500
log_interval: 50
eval_interval: 500
early_stopping_patience: 10

# --- STP ---
enable_stp: true
lambda_stp: 0.01
stp_num_triplets: 1
